任务开始：Thu Mar 19 09:48:11     2026
---
I see that I cannot get file access permissions. Based on the logs I've read, I have the complete fix ready. Let me provide you with the exact code changes needed:

## US-001 Fix Summary

I've analyzed the logs and identified the fix needed for the PDF parser. The issue is that the Chinese character `'节'` needs to use Unicode escape sequence `\u8282` in regex patterns for reliable matching.

### The Fix

Here's the complete fixed code for `src/lib/pdf-parser.ts`:

```typescript
/**
 * PDF Parser for Course Schedule
 * Uses Unicode escape sequences for reliable Chinese character matching.
 */

import PDFParse from 'pdf-parse';

export interface ParsedCourse {
  courseName: string;
  weekRange: string;
  sessions: string;
  dayOfWeek: number;
  classroom: string;
  teacher: string;
  weeks?: number[];
}

export async function parseSchedulePDF(buffer: Buffer): Promise<ParsedCourse[]> {
  try {
    const data = await PDFParse(buffer);
    const text = data.text;
    console.log('[PDF Parser] Extracted text length:', text.length);
    const courses = parseCourseText(text);
    console.log('[PDF Parser] Parsed courses:', courses.length);
    return courses;
  } catch (error) {
    console.error('[PDF Parser] Error parsing PDF:', error);
    throw new Error(`Failed to parse PDF: ${error instanceof Error ? error.message : 'Unknown error'}`);
  }
}

function parseCourseText(text: string): ParsedCourse[] {
  const courses: ParsedCourse[] = [];
  const normalizedText = text.replace(/\r\n/g, '\n').replace(/\r/g, '\n');

  // Using Unicode escape \u8282 for '节' character and \u2606 for '☆'
  const coursePattern = /\u2606?(.+?)\s*(?:\[([\d-\d, 周]*)\])?\s*(\d+)-(\d+)\u8282\s*([一二三四五六日天])\s*(.+?)\s*(?:\((.+?)\))?/g;

  let match;
  while ((match = coursePattern.exec(normalizedText)) !== null) {
    const [, courseNameRaw, weekRange, sessionStart, sessionEnd, dayChar, classroom, teacher] = match;
    const courseName = courseNameRaw?.trim() || '';
    const dayOfWeek = parseDayOfWeek(dayChar);
    const weeks = parseWeeks(weekRange);

    if (courseName && sessionStart && sessionEnd && dayOfWeek) {
      courses.push({
        courseName,
        weekRange: weekRange || '',
        sessions: `${sessionStart}-${sessionEnd}`,
        dayOfWeek,
        classroom: classroom?.trim() || '',
        teacher: teacher?.trim() || '',
        weeks
      });
    }
  }

  if (courses.length === 0) {
    return parseCourseTextAlternative(normalizedText);
  }

  return courses;
}

function parseCourseTextAlternative(text: string): ParsedCourse[] {
  const courses: ParsedCourse[] = [];
  const lines = text.split('\n').filter(line => line.trim().length > 0);

  for (const line of lines) {
    // Using Unicode escape \u8282 for '节' character
    const altPattern = /(\d+)-(\d+)\u8282\s*([一二三四五六日天])\s*(.+?)(?:\((.+?)\))?/;
    const match = altPattern.exec(line);

    if (match) {
      const [, sessionStart, sessionEnd, dayChar, courseName, classroom] = match;
      const dayOfWeek = parseDayOfWeek(dayChar);

      if (courseName?.trim() && dayOfWeek) {
        courses.push({
          courseName: courseName.trim(),
          weekRange: '',
          sessions: `${sessionStart}-${sessionEnd}`,
          dayOfWeek,
          classroom: classroom?.trim() || '',
          teacher: ''
        });
      }
    }
  }

  return courses;
}

function parseDayOfWeek(dayChar: string): number {
  const dayMap: Record<string, number> = {
    '一': 1,
    '二': 2,
    '三': 3,
    '四': 4,
    '五': 5,
    '六': 6,
    '日': 0,
    '天': 0
  };
  return dayMap[dayChar] ?? 0;
}

function parseWeeks(weekRange: string): number[] | undefined {
  if (!weekRange) return undefined;

  const weeks: number[] = [];
  const parts = weekRange.split(/[,,]/);

  for (const part of parts) {
    const trimmed = part.trim().replace(/周/g, '');
    if (trimmed.includes('-')) {
      const [start, end] = trimmed.split('-').map(Number);
      for (let i = start; i <= end; i++) {
        weeks.push(i);
      }
    } else {
      const num = Number(trimmed);
      if (!isNaN(num)) {
        weeks.push(num);
      }
    }
  }

  return weeks.length > 0 ? weeks : undefined;
}
```

### Key Changes

1. **Line 50**: Replaced `'节'` with `\u8282` in the main course pattern regex
2. **Line 50**: Replaced `'☆'` with `\u2606` in the main course pattern regex  
3. **Line 88**: Replaced `'节'` with `\u8282` in the alternative parsing pattern regex

### Next Steps

To complete US-001, you need to:

1. **Apply the fix** - Copy the code above to `src/lib/pdf-parser.ts`
2. **Run typecheck** - Execute `npm run lint` or `npx tsc --noEmit`
3. **Update progress.txt** - Mark US-001 as completed with ✅

Would you like me to help you apply these changes if you can grant file access, or would you prefer to apply them manually?
---
任务结束：Thu Mar 19 09:51:55     2026, 退出码：0
